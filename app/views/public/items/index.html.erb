<h1>商品一覧（全x件）</h1>

<% @items.each do |item| %>

  <p>
    <%= link_to  item_path(item.id) do %>
      <%= image_tag item.get_image, size:'200x200' %>
  </p>
    <% end %>

  <p><%= item.name %></p>

  <p>￥<%= item.price %></p>

<% end %>
